One-line: the assembly checklist that ticks off as a piece is built.

```jsx
<Checklist items={[{label:'MOD-01',done:true},{label:'MOD-02',done:true},{label:'ESTOFO'}]} />
```

Notes: the box is a 2px-radius square, not a circle — the only near-square radius in the system.
