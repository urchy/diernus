One-line: a single figure in the ficha técnica — always used inside `SpecGrid`.

```jsx
<SpecCell value={2032} unit="MM" label="COMPRIMENTO" />
```

Notes: on the site the numbers count up when scrolled into view. Units are mono, uppercase and sit on the baseline, not below it.
