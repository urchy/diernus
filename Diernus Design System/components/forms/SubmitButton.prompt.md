One-line: the wide cobalt submit at the foot of a form — the one button that is not a pill.

```jsx
<SubmitButton>Enviar mensagem →</SubmitButton>
```

Notes: presses 1px down, never scales. Label is sentence-case Figtree, not mono.
