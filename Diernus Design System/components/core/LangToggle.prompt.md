One-line: the two-up PT/EN pill that sits at the right of every Diernus top bar.

```jsx
<LangToggle value={lang} onChange={setLang} />
```

Notes: the active side is solid graphite with bone text. Every Diernus surface must carry it — the studio works in PT and EN.
