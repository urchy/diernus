One-line: the rotated red approval stamp — use once per surface, at the end of a process.

```jsx
<Stamp label="R3 · VALIDADO" sub="28/05/2026 · DIERNUS" />
```

Notes: `--accent-stamp` (#B3232E) is reserved for approvals, revision numbers and "hot" chips. Never use it for errors.
