One-line: the brand mark — the word DIERNUS in letterspaced uppercase mono, never a drawn symbol.

```jsx
<Wordmark />
<Wordmark tone="ink" size="1.15rem" />
```

Notes: on bone the name is cobalt; on blueprint it is amber. In the footer bottom rule it appears unaccented at `--t-mono-sm`.
