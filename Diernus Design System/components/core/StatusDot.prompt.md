One-line: a small live-status line — amber dot with a soft halo plus mono copy.

```jsx
<StatusDot>Lemos tudo · respondemos por email</StatusDot>
```

Notes: lives on blueprint surfaces. On the real site the halo pulses every 2.2s; the pulse is suppressed under `prefers-reduced-motion`.
